[project]
name = "clcoevt"
version = "0.3.0"
description = "command options via commandline, environment variables and configuration files"
license = "MIT"
license-files = ["LICEN[CS]E*"]
authors = [
    { name = "HASHIGAYA, Makoto", email = "6903330+makooot@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.19,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "nox>=2025.11.12",
    "nox-uv>=0.6.3",
    "ruff>=0.14.3",
    "ty>=0.0.73",
]

[tool.ty.environment]
root = ["./src", "./test", "./test_package"]

[tool.ty.rules]
all = "error"
